# 📖 Git使用教程

> **学习目标**：掌握Git基本操作，学会代码版本管理和远程仓库协作

---

## 1. Git简介

**Git** 是一个分布式版本控制系统，可以：

- ✅ 追踪代码变更历史
- ✅ 多人协作开发
- ✅ 代码备份
- ✅ 分支管理

---

## 2. 环境检查

你的Git已经配置好了！

```powershell
# 检查Git版本
git --version
# 输出: git version 2.52.0.windows.1

# 检查用户配置
git config user.email
# 输出: 3237768167@qq.com
```

---

## 3. 常用命令

### 3.1 初始化仓库

```bash
# 在当前目录初始化Git仓库（已完成）
git init
```

### 3.2 查看状态

```bash
# 查看当前仓库状态
git status
```

### 3.3 添加文件

```bash
# 添加单个文件
git add filename.py

# 添加所有文件
git add .

# 添加特定类型
git add *.py
```

### 3.4 提交更改

```bash
# 提交（附带消息）
git commit -m "描述这次更改的内容"

# 示例
git commit -m "添加图书管理API基础功能"
```

### 3.5 查看历史

```bash
# 查看提交历史
git log

# 简洁格式
git log --oneline
```

---

## 4. 连接GitHub

### 4.1 创建GitHub账号

如果还没有，请访问 https://github.com 注册。

### 4.2 创建远程仓库

1. 登录GitHub
2. 点击右上角 "+" → "New repository"
3. 填写仓库名称（如：`ai-engineer-study`）
4. 不要勾选 "Initialize with README"
5. 点击 "Create repository"

### 4.3 连接远程仓库

```bash
# 添加远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/ai-engineer-study.git

# 验证远程仓库
git remote -v
```

### 4.4 首次推送

```bash
# 推送到远程仓库
git push -u origin main

# 如果提示分支名是master，改用：
git push -u origin master
```

---

## 5. 日常工作流

```bash
# 1. 查看状态
git status

# 2. 添加更改
git add .

# 3. 提交
git commit -m "完成Week 1学习"

# 4. 推送
git push
```

---

## 6. 常见问题

### Q1: 推送时要求输入密码？

GitHub已停止密码认证，需要使用Personal Access Token：

1. 访问 https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. 勾选 "repo" 权限
4. 生成token并保存
5. 推送时使用token代替密码

### Q2: 如何撤销更改？

```bash
# 撤销未暂存的更改
git checkout -- filename

# 撤销暂存
git reset HEAD filename
```

### Q3: 如何查看差异？

```bash
# 查看未暂存的更改
git diff

# 查看已暂存的更改
git diff --cached
```

---

## 7. 当前项目操作

在你的项目中执行以下命令：

```powershell
cd "i:\Study FastAPI"

# 查看状态
git status

# 添加所有文件
git add .

# 提交
git commit -m "初始化AI工程师学习项目"

# 如果要推送到GitHub，先创建远程仓库，然后：
# git remote add origin https://github.com/你的用户名/仓库名.git
# git push -u origin main
```

---

## 8. Git速查表

| 命令 | 说明 |
|------|------|
| `git init` | 初始化仓库 |
| `git status` | 查看状态 |
| `git add .` | 添加所有文件 |
| `git commit -m "msg"` | 提交更改 |
| `git log` | 查看历史 |
| `git push` | 推送到远程 |
| `git pull` | 拉取更新 |
| `git clone url` | 克隆仓库 |
| `git diff` | 查看差异 |

---

**Git是每个开发者必备技能，多用就熟练了！💪**
